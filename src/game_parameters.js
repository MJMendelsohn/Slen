/**
 * The object used to define the structure of a given game.
 * @param size The integer size of the board.
 */
export function GameParameters(size) {
     GameParameters.size = size; // static for now to try to test out React
}
