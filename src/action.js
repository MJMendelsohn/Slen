/**
 * The object used to send actions from the View to the Model.
 * @param id The id of a given action.
 * @param contents The contents of a given action.
 */
function Action(id, contents) {
    this.id = id;
    this.contents = contents;
}